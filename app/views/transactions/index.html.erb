<h1>Hello from Chloe</h1>
<br>
<% @mytxns.each do |txn| %>
  <h4>Transaction No: <%= txn.id %></h4>
  <% txn_ticker = Ticker.find(txn.user_ticker.ticker_id) %>
  <p><%= txn_ticker.name %> x <%= txn.qty %> qty (<%= txn.flow %>)</p>
<% end %>
