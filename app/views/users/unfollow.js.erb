document.getElementById("follow-button").innerHTML = "<%= escape_javascript(link_to 'Follow', follow_user_path(@user), method: :post, remote: true, class: 'btn btn-sm btn-primary ml-2', data: { controller: 'follow', action: 'click->follow#follow', user_id: @user.id }) %>";
