document.getElementById("follow-button").innerHTML = "<%= escape_javascript(link_to 'Unfollow', unfollow_user_path(@user), method: :delete, remote: true, class: 'btn btn-sm btn-danger ml-2', data: { controller: 'unfollow', action: 'click->unfollow#unfollow', user_id: @user.id }) %>";
